# Safe Grammarly

## Get started

Create a copy of the `.env.example` file:

```bash
cp .env.example .env
```

Install dependencies:

```bash
pnpm install
```

Start the development server:

```bash
pnpm dev
```